name: Frontend Auto-Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'admin-dashboard/**'
      - 'franchise-dashboard/**'
      - 'franchise-register/**'
      - 'estimate-keep-system/**'
      - 'lp/**'
      - 'js/**'
      - '.github/workflows/**'
  workflow_dispatch: # 手動実行も可能

permissions:
  contents: write

jobs:
  deploy-admin:
    name: Deploy Admin Dashboard
    runs-on: ubuntu-latest
    needs: [deploy-js]
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Auto-increment JS version numbers
        run: |
          echo "🔢 バージョン番号を自動インクリメント中..."
          JS_FILE="admin-dashboard/js/business-selection-handler.js"

          if [ -f "$JS_FILE" ]; then
            # 現在のバージョン番号を取得
            CURRENT_VERSION=$(grep -E "const BUSINESS_SELECTION_HANDLER_VERSION = [0-9]+" "$JS_FILE" | grep -oE "[0-9]+" | head -1)
            echo "現在のバージョン: V$CURRENT_VERSION"

            # バージョンをインクリメント
            NEW_VERSION=$((CURRENT_VERSION + 1))
            echo "新しいバージョン: V$NEW_VERSION"

            # JSファイル内のバージョン番号を更新（2箇所）
            sed -i.bak -E "s/const BUSINESS_SELECTION_HANDLER_VERSION = [0-9]+;/const BUSINESS_SELECTION_HANDLER_VERSION = $NEW_VERSION;/" "$JS_FILE"
            sed -i.bak -E "s/const EXPECTED_MIN_VERSION = [0-9]+;/const EXPECTED_MIN_VERSION = $NEW_VERSION;/" "$JS_FILE"
            rm "$JS_FILE.bak"

            echo "✅ V$NEW_VERSION に更新完了"
            echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV
          else
            echo "⚠️ $JS_FILE が見つかりません"
          fi

      - name: Update cache busters in HTML files
        run: |
          echo "🔄 Updating cache busters in HTML files..."
          echo "NEW_VERSION from previous step: ${NEW_VERSION}"

          # business-selection-handler.js は NEW_VERSION を使用（JSバージョンと一致）
          # 他の.jsファイルはタイムスタンプを使用
          TIMESTAMP_CACHE_BUSTER="v$(date +%s)"
          VERSION_CACHE_BUSTER="V${NEW_VERSION}"

          echo "Timestamp cache buster: $TIMESTAMP_CACHE_BUSTER"
          echo "Version cache buster: $VERSION_CACHE_BUSTER"

          # admin-dashboard/index.html のキャッシュバスターを更新
          # V1933: 動的ロード部分（' + cacheBuster + '）を破壊しないよう、静的パターンのみ置換
          if [ -f "admin-dashboard/index.html" ]; then
            # 静的な .js?v=XXX" パターンのみ置換（動的ロードの ' + cacheBuster + ' を除外）
            # パターン: .js?v= の後に英数字・ハイフン・アンダースコアのみ（+やシングルクォート含まない）
            sed -i.bak -E "s|\.js\?v=[A-Za-z0-9_-]+\"|.js?v=$TIMESTAMP_CACHE_BUSTER\"|g" admin-dashboard/index.html
            # business-selection-handler.js のみバージョン番号で上書き
            sed -i.bak -E "s|business-selection-handler\.js\?v=[A-Za-z0-9_-]+\"|business-selection-handler.js?v=$VERSION_CACHE_BUSTER\"|g" admin-dashboard/index.html
            rm -f admin-dashboard/index.html.bak
            echo "✅ Updated admin-dashboard/index.html"
            echo "  - business-selection-handler.js: $VERSION_CACHE_BUSTER"
            echo "  - Other JS files: $TIMESTAMP_CACHE_BUSTER"
            echo "  - Dynamic loading preserved (cv-list-manager.js, fee-calculator.js)"
          fi

      - name: Commit and push version updates
        continue-on-error: true
        run: |
          echo "📝 バージョン更新をコミット中..."
          echo "NEW_VERSION from env: ${NEW_VERSION}"

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          echo "=== Git Status Before Add ==="
          git status

          git add admin-dashboard/js/business-selection-handler.js
          git add admin-dashboard/index.html || echo "⚠️ index.html add failed, continuing..."

          echo "=== Git Status After Add ==="
          git status

          echo "=== Git Diff Staged ==="
          git diff --staged --name-only

          if git diff --staged --quiet; then
            echo "📍 変更なし - コミットスキップ"
            echo "=== Working tree files ==="
            cat admin-dashboard/js/business-selection-handler.js | head -60 | grep "VERSION =" || echo "VERSION check failed"
          else
            echo "📝 変更を検出 - コミットします"
            git commit -m "auto: V${NEW_VERSION} + cache buster update - Admin Dashboard" || echo "⚠️ Commit failed, continuing..."
            git push || echo "⚠️ Push failed, continuing to deployment..."
            echo "✅ バージョン更新処理完了"
          fi

          echo "📍 FTPデプロイに進みます..."

      - name: Deploy to Xserver via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./admin-dashboard/
          server-dir: gaihekikuraberu.com/public_html/admin-dashboard/
          dangerous-clean-slate: false

      - name: Verify deployment
        run: |
          sleep 5
          curl -f https://gaihekikuraberu.com/admin-dashboard/ || exit 1
          echo "✅ Admin Dashboard deployed successfully"

  deploy-franchise-dashboard:
    name: Deploy Franchise Dashboard
    runs-on: ubuntu-latest
    needs: [deploy-js]
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Update cache busters in HTML files
        run: |
          echo "🔄 Updating cache busters in HTML files..."
          CACHE_BUSTER="v$(date +%s)"
          echo "Cache buster: $CACHE_BUSTER"

          # franchise-dashboard/index.html のキャッシュバスターを更新
          if [ -f "franchise-dashboard/index.html" ]; then
            sed -i.bak -E "s/\.js\?v=[^\"]+/.js?v=$CACHE_BUSTER/g" franchise-dashboard/index.html
            rm franchise-dashboard/index.html.bak
            echo "✅ Updated franchise-dashboard/index.html"
          fi

      - name: Commit and push cache buster updates
        run: |
          echo "📝 キャッシュバスター更新をコミット中..."

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          git add franchise-dashboard/index.html

          if git diff --staged --quiet; then
            echo "📍 変更なし - コミットスキップ"
          else
            git commit -m "auto: cache buster update - Franchise Dashboard"
            git push
            echo "✅ キャッシュバスター更新をプッシュしました"
          fi

      - name: Deploy to Xserver via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./franchise-dashboard/
          server-dir: gaihekikuraberu.com/public_html/franchise-dashboard/
          dangerous-clean-slate: false

      - name: Verify deployment
        run: |
          sleep 5
          curl -f https://gaihekikuraberu.com/franchise-dashboard/ || exit 1
          echo "✅ Franchise Dashboard deployed successfully"

  deploy-register:
    name: Deploy Franchise Register
    runs-on: ubuntu-latest
    needs: [deploy-js]
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Update cache busters in HTML files
        run: |
          echo "🔄 Updating cache busters in HTML files..."
          CACHE_BUSTER="v$(date +%s)"
          echo "Cache buster: $CACHE_BUSTER"

          # franchise-register/index.html のキャッシュバスターを更新
          if [ -f "franchise-register/index.html" ]; then
            sed -i.bak -E "s/\.js\?v=[^\"]+/.js?v=$CACHE_BUSTER/g" franchise-register/index.html
            rm franchise-register/index.html.bak
            echo "✅ Updated franchise-register/index.html"
          fi

      - name: Commit and push cache buster updates
        run: |
          echo "📝 キャッシュバスター更新をコミット中..."

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          git add franchise-register/index.html

          if git diff --staged --quiet; then
            echo "📍 変更なし - コミットスキップ"
          else
            git commit -m "auto: cache buster update - Franchise Register"
            git push
            echo "✅ キャッシュバスター更新をプッシュしました"
          fi

      - name: Deploy to Xserver via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./franchise-register/
          server-dir: gaihekikuraberu.com/public_html/franchise-register/
          exclude: |
            **/.git*
            **/.git*/**
            **/gas/**
            **/node_modules/**
            **/.DS_Store
          dangerous-clean-slate: false

      - name: Verify deployment
        run: |
          sleep 5
          curl -f https://gaihekikuraberu.com/franchise-register/ || exit 1
          echo "✅ Franchise Register deployed successfully"

  deploy-estimate:
    name: Deploy Estimate System
    runs-on: ubuntu-latest
    needs: [deploy-js]
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Update cache busters in HTML files
        run: |
          echo "🔄 Updating cache busters in HTML files..."
          CACHE_BUSTER="v$(date +%s)"
          echo "Cache buster: $CACHE_BUSTER"

          # estimate-keep-system/index.html のキャッシュバスターを更新
          if [ -f "estimate-keep-system/index.html" ]; then
            sed -i.bak -E "s/\.js\?v=[^\"]+/.js?v=$CACHE_BUSTER/g" estimate-keep-system/index.html
            rm estimate-keep-system/index.html.bak
            echo "✅ Updated estimate-keep-system/index.html"
          fi

      - name: Commit and push cache buster updates
        run: |
          echo "📝 キャッシュバスター更新をコミット中..."

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          git add estimate-keep-system/index.html

          if git diff --staged --quiet; then
            echo "📍 変更なし - コミットスキップ"
          else
            git commit -m "auto: cache buster update - Estimate System"
            git push
            echo "✅ キャッシュバスター更新をプッシュしました"
          fi

      - name: Deploy to Xserver via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./estimate-keep-system/
          server-dir: gaihekikuraberu.com/public_html/estimate-keep-system/
          dangerous-clean-slate: false

      - name: Verify deployment
        run: |
          sleep 5
          curl -f https://gaihekikuraberu.com/estimate-keep-system/ || exit 1
          echo "✅ Estimate System deployed successfully"

  deploy-lp:
    name: Deploy LP
    runs-on: ubuntu-latest
    needs: [deploy-js]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Update cache busters in HTML files
        run: |
          echo "🔄 Updating cache busters in HTML files..."
          CACHE_BUSTER="v$(date +%s)"
          echo "Cache buster: $CACHE_BUSTER"

          # lp/index.html のキャッシュバスターを更新
          if [ -f "lp/index.html" ]; then
            # JS/CSSの静的キャッシュバスター更新（?v=XXXX 形式）
            sed -i.bak -E "s/\.js\?v=[0-9]+\"/.js?v=$CACHE_BUSTER\"/g" lp/index.html
            sed -i.bak -E "s/\.css\?v=[0-9]+\"/.css?v=$CACHE_BUSTER\"/g" lp/index.html
            rm -f lp/index.html.bak
            echo "✅ Updated lp/index.html"
          fi

      - name: Commit and push cache buster updates
        run: |
          echo "📝 キャッシュバスター更新をコミット中..."

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          git add lp/index.html

          if git diff --staged --quiet; then
            echo "📍 変更なし - コミットスキップ"
          else
            git commit -m "auto: cache buster update - LP"
            git push
            echo "✅ キャッシュバスター更新をプッシュしました"
          fi

      - name: Deploy to Xserver via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./lp/
          server-dir: gaihekikuraberu.com/public_html/lp/
          dangerous-clean-slate: false
          log-level: verbose

      - name: Verify deployment
        run: |
          sleep 5
          curl -f https://gaihekikuraberu.com/lp/ || exit 1
          echo "✅ LP deployed successfully"

  deploy-js:
    name: Deploy JS (env-loader) - Updates cache buster for all frontend deploys
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Update env-loader.js CACHE_BUSTER
        run: |
          echo "🔄 Updating env-loader.js CACHE_BUSTER..."
          CACHE_BUSTER="v$(date +%s)-fe$(echo $GITHUB_SHA | cut -c1-6)"
          echo "Cache buster: $CACHE_BUSTER"

          if [ -f "js/env-loader.js" ]; then
            # CACHE_BUSTER の値を更新
            sed -i.bak -E "s/CACHE_BUSTER: '[^']+'/CACHE_BUSTER: '$CACHE_BUSTER'/" js/env-loader.js
            # コメントも更新
            sed -i.bak -E "s|// キャッシュバスター\([^)]*\)|// キャッシュバスター（Frontend Auto-Deploy $(date +%Y-%m-%d)）|" js/env-loader.js
            rm -f js/env-loader.js.bak
            echo "✅ Updated js/env-loader.js with CACHE_BUSTER: $CACHE_BUSTER"
            grep "CACHE_BUSTER" js/env-loader.js
          fi

      - name: Commit and push cache buster updates
        run: |
          echo "📝 env-loader.js キャッシュバスター更新をコミット中..."

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          # マスターenv-loader.jsのみステージング（全システムが/js/env-loader.jsを参照）
          git add js/env-loader.js

          if git diff --staged --quiet; then
            echo "📍 変更なし - コミットスキップ"
          else
            git commit -m "auto: cache buster update - /js/env-loader.js (master)"
            git push
            echo "✅ マスターenv-loader.jsの更新をプッシュしました"
          fi

      - name: Deploy to Xserver via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./js/
          server-dir: gaihekikuraberu.com/public_html/js/
          dangerous-clean-slate: false
          log-level: verbose

      - name: Verify deployment
        run: |
          sleep 5
          curl -f https://gaihekikuraberu.com/js/env-loader.js || exit 1
          echo "✅ JS directory deployed successfully"

  notify:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [deploy-admin, deploy-franchise-dashboard, deploy-register, deploy-estimate, deploy-lp, deploy-js]
    if: always()

    steps:
      - name: Check deployment status
        run: |
          echo "🎉 All frontend deployments completed!"
          echo ""
          echo "Deployed URLs:"
          echo "  - Admin: https://gaihekikuraberu.com/admin-dashboard/"
          echo "  - Franchise Dashboard: https://gaihekikuraberu.com/franchise-dashboard/"
          echo "  - Register: https://gaihekikuraberu.com/franchise-register/"
          echo "  - Estimate: https://gaihekikuraberu.com/estimate-keep-system/"
          echo "  - LP: https://gaihekikuraberu.com/lp/"
