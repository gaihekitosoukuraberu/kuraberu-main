name: GAS CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'franchise-register/gas/**'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'franchise-register/gas/**'
  workflow_dispatch: # 手動実行も可能

permissions:
  contents: write

env:
  GAS_DIR: franchise-register/gas

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Syntax check for all JS files
        working-directory: ${{ env.GAS_DIR }}
        run: |
          echo "🔍 JavaScript構文チェック開始"
          find . -name "*.js" -type f | while read file; do
            echo "Checking: $file"
            node -c "$file" || exit 1
          done
          echo "✅ 全てのJavaScriptファイルの構文チェック完了"

      - name: Check required files exist
        working-directory: ${{ env.GAS_DIR }}
        run: |
          echo "📁 必須ファイルの存在確認"
          test -f main.js || (echo "❌ main.js が見つかりません" && exit 1)
          test -f .clasp.json || (echo "❌ .clasp.json が見つかりません" && exit 1)
          test -d systems || (echo "❌ systems ディレクトリが見つかりません" && exit 1)
          echo "✅ 必須ファイル確認完了"

  # 【重要】V1758固定運用 - 組織アカウント制限で@HEAD不可
  # 詳細: franchise-register/gas/DEPLOYMENT_CHECKLIST.md 参照
  deploy:
    name: Deploy to GAS (V1758 fixed)
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Setup clasp credentials
        working-directory: ${{ env.GAS_DIR }}
        run: |
          echo '${{ secrets.CLASP_CREDENTIALS }}' > ~/.clasprc.json
          # .clasp.jsonはリポジトリのものを使用（上書きしない）

      - name: Debug - Check files
        working-directory: ${{ env.GAS_DIR }}
        run: |
          echo "📍 Current directory: $(pwd)"
          echo "📁 Files in current directory:"
          ls -la
          echo "📄 .clasp.json content:"
          cat .clasp.json
          echo "🔑 .clasprc.json exists: $(test -f ~/.clasprc.json && echo 'YES' || echo 'NO')"

      - name: Push to GAS and use V1758 deployment
        id: deploy
        working-directory: ${{ env.GAS_DIR }}
        run: |
          echo "🚀 GASにコードをプッシュ中..."
          clasp push --force

          echo "📍 V1758デプロイメント使用（組織アカウント制限のため@HEAD不可）"
          # V1758の固定デプロイメントIDを使用
          DEPLOYMENT_ID="AKfycbzzFPNZ3uO9InJlzXGaWIT3y0uALwVHY9miqtQKuMsWQNRTUw2KQAGynf5X5C-Nk42jvQ"
          echo "deployment_id=$DEPLOYMENT_ID" >> $GITHUB_OUTPUT
          echo "✅ V1758 Deployment ID: $DEPLOYMENT_ID"

      - name: Health check
        run: |
          DEPLOYMENT_ID="${{ steps.deploy.outputs.deployment_id }}"
          echo "⏳ 新しいデプロイメントが有効になるまで15秒待機..."
          sleep 15

          echo "🔍 ヘルスチェック実行中..."
          RESPONSE=$(curl -sL "https://script.google.com/macros/s/$DEPLOYMENT_ID/exec?action=health&callback=test")
          echo "Health check response: $RESPONSE"

          if echo "$RESPONSE" | grep -q '"success":true'; then
            echo "✅ Deployment successful!"
          else
            echo "❌ Deployment failed health check"
            exit 1
          fi

      - name: Log deployment success
        run: |
          echo "✅ デプロイ成功"
          echo "Version: ${{ steps.deploy.outputs.version }}"
          echo "Deployment ID: ${{ steps.deploy.outputs.deployment_id }}"

      - name: Sync master env-loader.js (Single Source)
        run: |
          echo "🔄 マスターenv-loader.jsを最新GAS URLに同期中..."
          DEPLOYMENT_ID="${{ steps.deploy.outputs.deployment_id }}"
          node sync-master-env-loader.js "$DEPLOYMENT_ID"

      - name: Commit and push env-loader updates
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # マスターenv-loader.jsをステージング
          git add js/env-loader.js || true

          # 各ディレクトリのenv-loader.jsもステージング
          git add admin-dashboard/js/env-loader.js || true
          git add franchise-register/js/env-loader.js || true
          git add franchise-dashboard/merchant-portal/env-loader.js || true
          git add estimate-keep-system/js/env-loader.js || true

          # LPフォールバックURLファイルもステージング
          git add lp/js/cv-api.js || true
          git add lp/js/utils.js || true

          # 変更があればコミット
          if git diff --staged --quiet; then
            echo "📝 マスターenv-loader.js更新なし"
            echo "ENV_UPDATED=false" >> $GITHUB_ENV
          else
            git commit -m "auto: GAS URL同期 (master env-loader & fallback) - ${{ steps.deploy.outputs.deployment_id }}"
            git pull --rebase origin main
            git push
            echo "✅ マスターenv-loader.js更新をpushしました"
            echo "ENV_UPDATED=true" >> $GITHUB_ENV
          fi

      # ======================================================================
      # 🚀 env-loader.js更新後、全フロントエンドを本番サーバーに自動デプロイ
      # ======================================================================
      - name: Deploy updated env-loader.js to production servers
        if: env.ENV_UPDATED == 'true'
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: gaihekikuraberu.com/public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/gas/**
            **/node_modules/**
            **/.DS_Store
            **/vendor/**
            **/*.md
            **/.github/**
          dangerous-clean-slate: false
          log-level: minimal

      - name: Verify production deployment
        if: env.ENV_UPDATED == 'true'
        run: |
          echo "🔍 本番サーバーのマスターenv-loader.js更新を確認中..."
          sleep 10

          # マスターenv-loader.jsを確認
          PROD_DEPLOYMENT=$(curl -s "https://gaihekikuraberu.com/js/env-loader.js?$(date +%s)" | grep "Deployment:" | head -1)
          echo "本番サーバー: $PROD_DEPLOYMENT"
          echo "期待値: Deployment: ${{ steps.deploy.outputs.deployment_id }}"

          if echo "$PROD_DEPLOYMENT" | grep -q "${{ steps.deploy.outputs.deployment_id }}"; then
            echo "✅ 本番サーバーに正常にデプロイされました"
          else
            echo "⚠️ 本番サーバーの更新確認失敗（キャッシュの可能性があります）"
          fi

  rollback:
    name: Rollback on Failure
    runs-on: ubuntu-latest
    needs: deploy
    if: failure()

    steps:
      - name: Log failure
        run: |
          echo "================================"
          echo "❌ デプロイ失敗"
          echo "================================"
          echo "失敗したコミット: ${{ github.sha }}"
          echo "ブランチ: ${{ github.ref }}"
          echo ""
          echo "注: @HEADを使用しているため、GASのコードは前のバージョンのまま安全です"
          echo "詳細: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "================================"
